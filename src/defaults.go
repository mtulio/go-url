package main

var (
	/* Globals */
	config URLConfig

	defaultTimeout = 5
	defaultSchema  = "http"
	defaultURLPath = "/"

	/* Input options */
	configFile *string
)

/* Initialize */
func init() {
	/* Envs */
}
